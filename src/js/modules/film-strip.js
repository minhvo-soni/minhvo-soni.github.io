// ─── Social Section — Film Strip ─────────────────────────────────
// Handles: lazy-load, hover/tap-to-play, active focus scroll,
// brand-nav tabs, arrow buttons, duration badges.
// Source extracted from the inline <script> block in index.html.

// Populated on demand when refactoring out of index.html.
