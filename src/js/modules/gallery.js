// ─── Work / Gallery Carousel ─────────────────────────────────────
// Handles: goTo(idx), HUD dots, keyboard nav, touch swipe.
// Constants: N (auto), PER_VENUE = 1, VENUES[], getCardW()
// Source extracted from the inline <script> block in index.html.

// Populated on demand when refactoring out of index.html.
